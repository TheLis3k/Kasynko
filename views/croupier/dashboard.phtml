<?php $pageTitle = t('croupier.title'); require dirname(__DIR__) . '/layout/header.phtml'; ?>
<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:1.5rem">
    <h1 style="font-size:1.4rem;color:var(--color-gold)"><?= t('croupier.title') ?></h1>
    <span style="color:var(--color-text-muted);font-size:.85rem"><?= t('croupier.desc') ?></span>
</div>

<div class="card">
    <div class="card__title"><?= t('admin.game_stats') ?></div>
    <div class="table-wrap">
        <table>
            <thead>
                <tr>
                    <th><?= t('bets.game') ?></th>
                    <th><?= t('admin.total_sessions') ?></th>
                    <th><?= t('bets.total_staked') ?></th>
                    <th><?= t('bets.total_payout') ?></th>
                    <th><?= t('admin.total_revenue') ?></th>
                </tr>
            </thead>
            <tbody>
            <?php foreach ($gameStats as $gs): ?>
                <tr>
                    <td><?= e($gs['name']) ?></td>
                    <td><?= (int)$gs['sessions'] ?></td>
                    <td><?= number_format((float)$gs['total_staked'], 2) ?></td>
                    <td><?= number_format((float)$gs['total_payout'], 2) ?></td>
                    <td><?= number_format((float)$gs['revenue'], 2) ?></td>
                </tr>
            <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>

<div class="card">
    <div class="card__title"><?= t('admin.user_activity') ?></div>
    <div class="table-wrap">
        <table>
            <thead>
                <tr>
                    <th><?= t('auth.first_name') ?> <?= t('auth.last_name') ?></th>
                    <th><?= t('auth.email') ?></th>
                    <th><?= t('admin.total_sessions') ?></th>
                    <th><?= t('bets.total_staked') ?></th>
                    <th><?= t('admin.last_played') ?></th>
                </tr>
            </thead>
            <tbody>
            <?php foreach ($activity as $u): ?>
                <tr>
                    <td><?= e($u['first_name'] . ' ' . $u['last_name']) ?></td>
                    <td><?= e($u['email']) ?></td>
                    <td><?= (int)$u['sessions'] ?></td>
                    <td><?= number_format((float)$u['total_staked'], 2) ?></td>
                    <td><?= $u['last_played'] ? e(substr($u['last_played'], 0, 16)) : '—' ?></td>
                </tr>
            <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>

<div class="card">
    <div class="card__title"><?= t('admin.audit_log') ?></div>
    <div class="table-wrap">
        <table>
            <thead>
                <tr>
                    <th><?= t('audit.date') ?></th>
                    <th><?= t('audit.actor') ?></th>
                    <th><?= t('audit.action') ?></th>
                    <th><?= t('audit.description') ?></th>
                </tr>
            </thead>
            <tbody>
            <?php if (empty($auditLog)): ?>
                <tr><td colspan="4" style="text-align:center;color:var(--color-text-muted);padding:1.5rem"><?= t('audit.no_entries') ?></td></tr>
            <?php else: ?>
                <?php foreach ($auditLog as $entry): ?>
                <tr>
                    <td style="white-space:nowrap;font-size:.8rem"><?= e(substr($entry['created_at'], 0, 16)) ?></td>
                    <td><?= e($entry['actor_name']) ?></td>
                    <td><code style="font-size:.8rem"><?= e($entry['action']) ?></code></td>
                    <td style="font-size:.8rem;color:var(--color-text-muted)"><?= e($entry['description']) ?></td>
                </tr>
                <?php endforeach; ?>
            <?php endif; ?>
            </tbody>
        </table>
    </div>
</div>
<?php require dirname(__DIR__) . '/layout/footer.phtml'; ?>
